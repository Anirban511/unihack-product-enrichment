# --- API service -----------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
pydantic>=2.10
pydantic-settings>=2.7

# --- acquisition -----------------------------------------------------------
httpx>=0.28
requests>=2.32
brotli>=1.1
selenium>=4.30
beautifulsoup4>=4.13
lxml>=6.0
lxml_html_clean>=0.4
trafilatura>=2.2

# --- documents & reference data --------------------------------------------
pypdf>=5.0
pymupdf>=1.24
pandas>=2.2
openpyxl>=3.1

# --- matching, caching, LLM ------------------------------------------------
rapidfuzz>=3.14
diskcache>=5.6
groq>=1.0

# --- Hugging Face ZeroGPU --------------------------------------------------
# A ZeroGPU Space refuses to start unless it can detect a @spaces.GPU entry
# point. That detection happens at runtime, so the import must actually succeed;
# relying on the image to preinstall it left space_app.py silently falling into
# its except branch and registering nothing. Harmless on CPU, where the
# decorator is a pass-through.
spaces>=0.30

# --- front ends ------------------------------------------------------------
# gradio: the Hugging Face Space UI, mounted into the same FastAPI app
# streamlit: local use and the optional Streamlit Cloud deployment
gradio>=4.44
streamlit>=1.40
